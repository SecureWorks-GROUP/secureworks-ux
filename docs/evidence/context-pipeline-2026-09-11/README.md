# Context pipeline panel: local proof

User guidance and the panel contract are owned by [Ops Dashboard: Context pipeline and weekly review](../../../ops-dashboard.md#context-pipeline-and-weekly-review).

## Verification

- Initial implementation proof, before the later draft-preservation fix: seven focused behavioral Playwright tests passed, covering actual calls versus slots, honest unavailable status, insufficient/undrawn and 40-row samples, false-only invented-payment write/readback, escaped source content, signed-out refusal, actual `renderJarvisView` mounting with shipped auth/read helpers, and unknown business source date.
- Two synthetic screenshot tests passed at 1280 × 1000 and 390 × 844 viewports, including no horizontal overflow. Captures were opened and inspected. These are scoped component captures, not an authenticated full-app or production acceptance test.
- Existing job-detail auto-refresh regression: 12 passed, zero failed.
- Both inline JavaScript scripts parsed; `git diff --check` passed.
- Impeccable detector ran once: 61 incumbent findings, none in the new block/CSS. HTML parser packages were unavailable, so the detector fell back to regex and did not verify computed contrast or selector matching.
- The later draft-preservation fix adds regressions for reordered readback and week switching, failed readback, and concurrent server changes. The initial seven-test result above does not establish verification of that fix; this documentation phase ran no behavioral tests.
- Full signed-in integration remains unverified: B4 is not live in the verification environment. Synthetic evidence does not establish live backend authorisation or stop behavior.

The committed test extracts the actual panel, CSS, auth/read/write helpers and JARVIS renderer from `ops.html`. Every network request is aborted except explicitly intercepted synthetic endpoints. Fixtures contain no client personal data. Screenshots at `.impeccable/review/desktop.png` and `mobile.png` were generated by that test from the shipped component, using synthetic CSS token values and a small fixture shell. They are browser captures, not generated artwork.

Run in a dependency-equipped checkout: `npx playwright test tests/e2e/context-pipeline-panel.spec.js --workers=1`. This local checkout had no Playwright install; the run reused the installed package from the existing securesuite-v2 checkout and system Chrome with an isolated temporary profile. No dependency or lockfile changed. The default initial browser launch was sandbox-blocked; the isolated test run then succeeded with approved escalation.

No push, merge, deployment, provider operation or live data mutation occurred. No whole-repository test or real operator/backend integration is claimed.
